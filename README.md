Repository meant to replace my old monorepo, which got too messy for me to handle without receiving -10 emotional damage at each second.

Aiming to follow a new structure that allows a more organized variant of my personal chaos.

```
|_ chaos-dump // random backup of stuff I tried but don't wanna delete/lose just in case I need it. Don't expect it to compile.
|_ consumable // organized things suitable for public consumption with tutorials, proper documentation etc.
|_ typst // slides, papers, LaTeX & Typst
|_ wip // current work before it either gets miraculously promoted to `consumable` or becomes a backup in `chaos-dump` 
```
